-- ============================================================
-- V8: product.name 인덱스 추가 (자동완성 prefix 검색/이름 정렬 풀스캔 방지)
-- ============================================================
-- 자동완성은 prefix(name LIKE 'kw%') 조회이므로 B-tree 인덱스가 유효하다.
-- 한계: 목록 검색의 부분일치(name LIKE '%kw%')는 선행 와일드카드 때문에
--       이 인덱스를 활용하지 못하고 순차 스캔한다(부분일치 최적화는 별도 방식 필요).
CREATE INDEX idx_product_name ON product (name);
