spring:
  jpa:
    hibernate:
      ddl-auto: create   # 로컬 전용 — 서버 기동 시 테이블 재생성
    properties:
      hibernate:
        format_sql: true
        show_sql: true    # 로컬에서만 SQL 출력

logging:
  level:
    com.ecommerce: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
