# DB 설계

> **상태**: 🔲 템플릿 — Week 3 (Product Service 구현 시 작성)

---

## 배경
- 서비스별 DB 분리 이유 (Database per Service)
- PostgreSQL 선택 이유

## ERD
- 각 서비스 테이블 구조
- 관계도

## 인덱스 전략
- 어떤 컬럼에 인덱스를 걸었나
- 복합 인덱스 컬럼 순서 결정 근거
- 커버링 인덱스 적용 여부

## N+1 방지
- 어디서 N+1 발생 가능한가
- `fetch join` / `@EntityGraph` / `default_batch_fetch_size` 적용 위치

## 정규화 vs 비정규화
- 어떤 테이블을 비정규화 했나, 그 이유
- 주문 시점 상품 정보 스냅샷 처리

## 마이그레이션
- Flyway 사용
- 버전 관리 규칙

## 한계 및 향후 개선
- 샤딩, 파티셔닝 미적용 사유
- Read Replica 도입 시점 판단 기준

## 면접 답변 시나리오
> Q: 인덱스는 어떻게 설계했나요?
> A:

> Q: N+1 어떻게 방지했나요?
> A:

> Q: 서비스별 DB 분리하면 조인은 어떻게 하나요?
> A:
